syntax = "proto3";

import "google/api/annotations.proto";

package list;

service List {
  rpc GetElements(Request) returns (Response){
    option (google.api.http) = {
      post: "/v1/greeter"
      body: "*"
    };
  }
}
message Request {

}

message Response {
  repeated string list = 1;
}

